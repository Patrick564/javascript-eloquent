let tree = '';
let count = 0;

while (count < 7) {
    tree += '#';
    console.log(tree);
    count++;
}

/*
Book solution to this problem.

for (let line = '#'; line.length < 8; line += '#') {
    console.log(line)
}

*/
